import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'administrativo/156-belem',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
